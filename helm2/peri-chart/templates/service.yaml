apiVersion: v1
kind: Service
metadata:
  name: {{ include "peri-chart.fullname" . }}
  labels:
    {{- include "peri-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "peri-chart.selectorLabels" . | nindent 4 }}
