apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "peri-chart.fullname" . }}-test-connection"
  labels:
    {{- include "peri-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "peri-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
